{
  "name": "ls-get-life-science-products",
  "description": "Returns life sciences IV fluid products with stock, AUD pricing, and quantityPriceMatrix (line cost and canFulfill for quantities 1–4 in bag and case units). Optional productName and quantity for a specific availability check.",
  "handler": "handler.handler",
  "runtime": "nodejs_22.x",
  "input": {
    "type": "object",
    "properties": {
      "productName": {
        "type": "string",
        "description": "Product name or alias (Normal Saline, D5LR, saline, product 1). Aliases: product, search, query, productId."
      },
      "quantity": {
        "type": "number",
        "description": "Requested quantity for availability check. Aliases: qty."
      },
      "unit": {
        "type": "string",
        "description": "case, cases, bag, or bags. Defaults to bag when omitted."
      },
      "storePath": { "type": "string" }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "success": { "type": "boolean" },
      "products": { "type": "array" },
      "matchedProduct": { "type": "object" },
      "availabilityCheck": { "type": "object" },
      "lowStockProducts": { "type": "array" },
      "canFulfill": { "type": "boolean" },
      "lowStock": { "type": "boolean" },
      "pricing": { "type": "object" },
      "quantityPriceMatrix": { "type": "object" },
      "message": { "type": "string" },
      "error": { "type": "string" },
      "code": { "type": "string" }
    }
  }
}
