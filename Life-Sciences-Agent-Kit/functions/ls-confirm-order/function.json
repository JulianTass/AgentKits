{
  "name": "ls-confirm-order",
  "description": "Confirms a life sciences supply order (demo/open mode). Accepts customerId from ls_idv_customer even when that customer was created in a separate function deployment; optionally accountNumber and phone. Returns orderConfirmationNumber, orderTotalDisplay, and productName at root for Genesys translation maps.",
  "handler": "handler.handler",
  "runtime": "nodejs_22.x",
  "input": {
    "type": "object",
    "properties": {
      "customerId": {
        "type": "string",
        "description": "From ls_idv_customer. Preferred after identity check."
      },
      "uniqueIdentifier": { "type": "string" },
      "accountNumber": { "type": "string" },
      "phone": { "type": "string" },
      "lineItems": {
        "type": "array",
        "description": "Array of { productName, quantity, unit }. Or pass single productName, quantity, unit."
      },
      "productName": { "type": "string" },
      "quantity": { "type": "number" },
      "unit": { "type": "string" },
      "storePath": { "type": "string" }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "success": { "type": "boolean" },
      "orderConfirmationNumber": { "type": "string" },
      "orderNumber": { "type": "string" },
      "orderStatus": { "type": "string" },
      "customerId": { "type": "string" },
      "organizationName": { "type": "string" },
      "accountNumber": { "type": "string" },
      "orderTotalAud": { "type": "number" },
      "orderTotalDisplay": { "type": "string" },
      "productName": { "type": "string" },
      "quantity": { "type": "number" },
      "unit": { "type": "string" },
      "demoMode": { "type": "boolean" },
      "lines": { "type": "array" },
      "estimatedDeliveryWindow": { "type": "string" },
      "message": { "type": "string" },
      "error": { "type": "string" },
      "code": { "type": "string" },
      "details": { "type": "object" }
    }
  }
}
